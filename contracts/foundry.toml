[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
allow_paths = ["../node_modules"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

[rpc_endpoints]
sepolia = "${RPC_HTTP_URL}"

[fmt]
line_length = 100
tab_width = 4
